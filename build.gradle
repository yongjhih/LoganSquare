buildscript {
    repositories {
        jcenter()
    }
}

allprojects {
    group = 'com.infstory'
    version = '1.1.1'
}

subprojects {
    apply plugin: 'java'

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}
